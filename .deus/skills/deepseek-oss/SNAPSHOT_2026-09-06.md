# DeepSeek OSS Snapshot — 2026-09-06

Evidence state: `REMOTE_SOURCE_SNAPSHOT`; source trees are discoverable through GitHub, but this execution sandbox cannot perform outbound `git clone`, so per-repository HEAD pinning remains a live-runtime gate.

## Public repository surface observed

36 public repositories were returned for the `deepseek-ai` organization in this snapshot:

`deepseek-harness`, `DeepSeek-V3`, `DeepSeek-R1`, `awesome-deepseek-integration`, `DeepSeek-Coder`, `DeepSeek-OCR`, `Janus`, `FlashMLA`, `3FS`, `DeepEP`, `open-infra-index`, `DeepGEMM`, `DeepSeek-LLM`, `DeepSpec`, `DeepSeek-Coder-V2`, `awesome-deepseek-agent`, `DeepSeek-VL2`, `DeepSeek-V2`, `smallpond`, `Engram`, `DeepSeek-VL`, `DeepSeek-Math`, `DeepSeek-OCR-2`, `DreamCraft3D`, `DualPipe`, `DeepSeek-MoE`, `TileKernels`, `DeepSeek-V3.2-Exp`, `DeepSeek-Math-V2`, `EPLB`, `DeepSeek-Prover-V2`, `profile-data`, `awesome-deepseek-coder`, `ESFT`, `DeepSeek-Prover-V1.5`, `LPLB`.

The sync engine does not hard-code this list; it re-discovers the organization dynamically so future repositories become deltas.

## R1 — reconstructed capability spine from public evidence

- **Conditional compute:** DeepSeekMoE / fine-grained experts / shared experts.
- **Attention compression:** MLA; later sparse-attention/indexer work in V3.2-Exp.
- **Training/inference efficiency:** FP8, MTP, load balancing, DualPipe, DeepEP, DeepGEMM, FlashMLA, EPLB/LPLB.
- **Reasoning:** Math/Coder lineage, R1/R1-Zero RL, distillation, verifier-oriented Math-V2 / Prover work.
- **Modalities:** VL/Janus, OCR/OCR-2.
- **Infrastructure:** 3FS, smallpond, kernel/tooling repositories.
- **Agent runtime:** deepseek-harness with replaceable plugin/service seams and durable session/event architecture.

## Newly promoted 2026 realms

These are now first-class fingerprints in the skill registry, so future scans treat them as known regions and only analyze their deltas:

1. **Conditional memory / Engram** — deterministic scalable lookup as a sparsity axis complementary to MoE.
2. **Speculative decoding / DeepSpec** — full-stack data preparation, draft-model training and evaluation for speculative generation.
3. **Tile-level kernel realm** — MoE routing, low-precision quantization, Engram and Manifold HyperConnection kernels through TileKernels.
4. **Dynamic expert balancing / LPLB** — batch-level linear-programming redistribution tied to EPLB/DeepEP.
5. **Agent harness realm** — everything-is-a-plugin runtime, durable session log, capability seams and swappable model/tool adapters.
6. **Visual causal encoding / OCR-2** — visual causal flow and compressed visual-token document understanding.

## R2 — regression state

`PARTIAL_REMOTE_ONLY` in this sandbox. The engine is implemented to sample `HEAD`, `HEAD~1`, `HEAD~2` for every synced repository and isolate marker/file deltas. That pass must run in a DEUS runtime with GitHub network access before being marked `PROVEN`.

## R3 — high-value variant hypotheses

These are search targets, not claims that DeepSeek already implements them and not benchmark results:

- `conditional_memory_engram + sparse_attention + speculative_decoding`
- `self_verification + speculative_decoding + formal_proving`
- `lp_expert_balancing + expert_parallel_comm + low_precision_quantization`
- `agent_plugin_runtime + durable_session_projection + conditional_memory_engram`
- `visual_causal_flow + conditional_memory_engram + sparse_attention`
- `manifold_hyperconnection + moe_fine_grained + multi_token_prediction`

The live R3 engine ranks only combinations absent from the scanned repository capability sets and labels every candidate `HYPOTHETICAL_VARIANT_NOT_BENCHMARKED`.
