# BL∞ — Handoff package 2026-08-29

Thư mục này là gói chuyển giao **không-canonical** để một phiên ChatGPT/mô hình mạnh hơn tiếp tục audit và tái kiến trúc BL∞ mà không phải đọc repo theo kiểu mò mẫm.

Đọc theo thứ tự:

1. `BL_INFINITY_HANDOFF_2026-08-29.md` — trạng thái toàn dự án, canonical spine, lỗi semantic/site hiện tại, yêu cầu tái dựng.
2. `NEXT_MODEL_PROMPT.md` — prompt bootstrap có thể copy sang phiên mới.
3. `handoff-manifest.json` — bản machine-readable của trạng thái chuyển giao.

Lưu ý: gói handoff không tự sửa học thuyết và không được dùng để thay thế claim registry. Nó khóa lại **trạng thái nhận thức về repo hiện tại**, đặc biệt các chỗ mà source đã tiến xa hơn website công khai.
