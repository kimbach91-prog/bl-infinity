name: DEUS Cloud Receptor

on:
  push:
    branches:
      - main
    paths:
      - '.deus/dispatch/inbox/**'

jobs:
  receptor:
    runs-on: ubuntu-latest
    steps:
      - name: Cloud receptor liveness probe
        run: echo "DEUS_CLOUD_RECEPTOR_OK"
